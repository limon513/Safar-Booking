const BOOKING_TYPE = {
    PENDING:'pending',
    SUCCESS:'success',
    CANCLED:'cancled',
};

const TICKET_STATUS = {
    PENDING:'pending',
    SENT:'sent',
}

module.exports = {
    BOOKING_TYPE,
    TICKET_STATUS,
}
